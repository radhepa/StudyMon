/* Original supplementary questions. Snippets assume required standard headers.
   C99/C11/C17 rules unless a question explicitly names a different standard. */
(function(){
var serial={};
function mc(ch,t,tag,q,answer,wrong,why,code){var n=serial[ch]=(serial[ch]||0)+1;var c=wrong.slice();var a=n%4;c.splice(a,0,answer);QBANK[ch].push({id:'c'+ch+'-ed4-'+String(n).padStart(3,'0'),chapter:ch,t:t,k:'mcq',tag:tag,q:q,c:c,a:a,why:why,code:code||''});}
function fill(ch,t,q,answer,why,code){var n=serial[ch]=(serial[ch]||0)+1;QBANK[ch].push({id:'c'+ch+'-ed4-'+String(n).padStart(3,'0'),chapter:ch,t:t,k:'fill',tag:'Trace',q:q,a:[String(answer)],why:why,code:code||''});}
var concepts=[
['What is a base case?','An input handled without another recursive call',['The first function in the file','A global variable','Any call with two arguments'],'A base case gives a direct answer and stops that branch of recursion.'],
['What must a terminating recursive step accomplish?','Move toward a reachable base case',['Always increase the argument','Call itself twice','Use a static variable'],'Choose a measure that decreases toward a bound, such as the number of elements left.'],
['What is direct recursion?','A function calls itself',['Two loops are nested','A function calls only library functions','A function has two returns'],'A call from f to f is direct recursion.'],
['What is indirect recursion?','A chain of calls eventually calls an earlier function again',['A pointer is always NULL','A loop contains a break','A function returns a pointer'],'For example, even calls odd and odd calls even. Both paths need terminating cases.'],
['What does each active recursive call normally have for its automatic local variables?','Its own instance',['One shared instance for all calls','Only zero values','The previous call overwrites them all'],'Automatic objects belong to each active invocation; a static local is different.'],
['Which local variable is shared across recursive calls?','A local declared static',['An ordinary int parameter','An automatic int local','Every local array'],'A static object has one lifetime spanning the program, rather than one instance per call.'],
['What is unwinding?','Returning from nested calls to their callers',['Deleting source files','Calling main repeatedly','Sorting the arguments'],'After a base case returns, suspended callers finish their remaining work.'],
['Which resource often limits deep recursion on ordinary implementations?','Call-stack space',['The number of source comments','The size of a file name','The number of include directives'],'Implementations commonly store active call state on a stack; C does not promise unlimited recursion.'],
['Does C guarantee tail-call optimization?','No',['Yes, for every recursive function','Yes, if the function returns int','Yes, whenever optimization is off'],'Do not assume a tail-recursive C function uses constant stack space.'],
['Which recurrence defines factorial for nonnegative integers?','fact(0)=1; fact(n)=n*fact(n-1) for n>0',['fact(0)=0; fact(n)=n*fact(n-1)','fact(n)=fact(n+1)','fact(n)=n+fact(n-1)'],'The identity value 1 lets multiplication build 1!, 2!, and later results.'],
['What is a recursive case?','A case solved using one or more smaller calls',['A case with no function call','The last statement in every program','A function prototype'],'Its calls reduce the work that remains; the caller may combine their results.'],
['Where should the NULL test go in a recursive list walk?','Before dereferencing the node pointer',['After reading node->next','Only after the final recursive call','It is unnecessary'],'The empty list is a natural base case and must be handled before accessing a node.'],
['What distinguishes total calls from maximum call depth?','Calls count all invocations; depth counts the largest simultaneous chain',['They are always equal','Depth counts only base cases','Calls count only unfinished functions'],'Branching recursion can make many calls while having a much smaller maximum depth.'],
['What should a recursive function specify about its inputs?','A domain for which its steps reach a base case',['Only the variable names','Only the return type size','Nothing beyond the function name'],'For example, repeated n-1 toward zero needs a suitable nonnegative input domain.'],
['Which is a natural empty-range base case for indices lo and hi, inclusive?','lo > hi',['lo < hi','lo != 0','hi == 100'],'With inclusive bounds, lo greater than hi means there are no elements left.'],
['When does code after a recursive call execute?','After that call returns',['Before the called function starts','Only in main','At the same instant in a new thread'],'Ordinary recursive calls are not automatically parallel. The caller waits for the result.']
];concepts.forEach(x=>mc(15,1,'Recall',x[0],x[1],x[2],x[3]));
var traces=[
['What does sum(4) return?',10,'4+3+2+1+0 is 10.','int sum(int n) { if (n == 0) return 0; return n + sum(n-1); }'],
['What does fact(5) return?',120,'5*4*3*2*1*1 is 120.','int fact(int n) { if (n == 0) return 1; return n * fact(n-1); }'],
['What is printed by down(3)? Use spaces between numbers.','3 2 1','Printing happens before each smaller call.','void down(int n) { if (n == 0) return; printf("%d ", n); down(n-1); }'],
['What is printed by up(3)? Use spaces between numbers.','1 2 3','Printing waits until the smaller call returns.','void up(int n) { if (n == 0) return; up(n-1); printf("%d ", n); }'],
['What does power(3, 3) return?',27,'Three recursive multiplications by 3 end at power(3,0)=1.','int power(int x,int n) { if(n==0) return 1; return x*power(x,n-1); }'],
['What does gcd(48,18) return?',6,'The argument pairs are (48,18), (18,12), (12,6), (6,0).','int gcd(int a,int b) { return b==0 ? a : gcd(b,a%b); }'],
['What does digits(507) return?',12,'The remainders are 7, 0, and 5.','int digits(int n) { return n==0 ? 0 : n%10+digits(n/10); }'],
['What does len("code") return?',4,'There are four non-NUL characters before the base case.','int len(const char *s) { return *s==\'\0\' ? 0 : 1+len(s+1); }'],
['What does fib(6) return?',8,'With fib(0)=0 and fib(1)=1, the sequence is 0,1,1,2,3,5,8.','int fib(int n) { return n<2 ? n : fib(n-1)+fib(n-2); }'],
['How many calls, including the initial call and base case, does f(4) make?',5,'The argument values are 4,3,2,1,0.','void f(int n) { if(n>0) f(n-1); }'],
['How many calls does f(3) make in total?',15,'T(0)=1 and T(n)=1+2*T(n-1), giving 1,3,7,15.','void f(int n) { if(n==0) return; f(n-1); f(n-1); }'],
['What is the maximum number of simultaneously active calls during f(3)?',4,'A deepest chain has n=3,2,1,0. The branches run one after the other.','void f(int n) { if(n==0) return; f(n-1); f(n-1); }'],
['What is printed by f(2)? Use spaces between numbers.','2 1 1 2','Each positive call prints once before and once after its child.','void f(int n) { if(n==0) return; printf("%d ",n); f(n-1); printf("%d ",n); }'],
['What does sum(a,3) return for int a[]={4,1,7}?',12,'The recursion adds a[2], a[1], then a[0].','int sum(const int *a,int n) { return n==0 ? 0 : a[n-1]+sum(a,n-1); }'],
['What does f(9) return?',4,'The positive values are 9,4,2,1; f(0) returns zero.','int f(int n) { return n==0 ? 0 : 1+f(n/2); }'],
['What does f(4,1) return?',24,'The accumulator changes to 4,12,24,24 before the final return.','int f(int n,int acc) { return n==0 ? acc : f(n-1,acc*n); }']
];traces.forEach(x=>fill(15,2,x[0],x[1],x[2],x[3]));
var bugs=[
['For positive n, why does this not terminate?','n never moves toward zero',['return must be void','The parameter must be static','C forbids recursion'],'The recursive call repeats the same problem.','int f(int n) { if(n==0) return 0; return f(n); }'],
['What is wrong with this factorial base case?','It makes every positive factorial result zero',['It creates a syntax error','It computes Fibonacci numbers','It works for every nonnegative n'],'Every multiplication eventually includes fact(0), which is zero here.','int fact(int n) { if(n==0) return 0; return n*fact(n-1); }'],
['Which input violates the intended domain of this countdown?','-1',['0','1','2'],'A negative n decreases away from zero. Signed overflow or resource exhaustion can follow.','void f(int n) { if(n==0) return; f(n-1); }'],
['Why is node->next unsafe here for an empty list?','node is dereferenced before it is checked',['next must be an int','NULL is only for arrays','The function returns too early'],'Test node for NULL before accessing a member.','void walk(struct Node *node) { walk(node->next); if(node==NULL) return; }'],
['Which change fixes the missing return on the recursive path?','Use return f(n-1);',['Change int to unsigned','Make n static','Add printf before the call'],'A non-void function whose result is used must supply a value on that path.','int f(int n) { if(n==0) return 7; f(n-1); }'],
['Why is f(n--) a bad recursive step toward zero?','The child receives the old n',['The child receives n-2','The program cannot compile','It always reaches zero sooner'],'The caller changes its own copy after providing the unchanged value to its child.','int f(int n) { if(n==0) return 0; return f(n--); }'],
['What is the problem with using a static accumulator for independent calls?','Later calls can inherit earlier state',['Static values reset on every call','The code always fails to compile','Parameters become global'],'An accumulator parameter or a fresh local wrapper avoids unintended cross-call state.'],
['Why can recursive factorial still fail with a correct base case?','Its result may overflow the chosen integer type',['Recursion prevents multiplication','Zero cannot be a base case','All factorials are undefined'],'Termination and numeric range are separate requirements. Signed overflow is undefined behavior.'],
['What prevents a recursive binary search from repeating the same interval?','Exclude the already-tested midpoint from the next interval',['Always set lo=mid','Keep both bounds unchanged','Only compare the array length'],'For inclusive bounds, recurse on lo..mid-1 or mid+1..hi.'],
['Why should the base case be checked before computing a[n-1]?','For n=0, a[n-1] is outside the range',['Arrays start at one','The compiler always adds a spare element','It makes all searches linear'],'The empty-range case must not access an element.'],
['What extra safeguard is needed for a recursive graph walk with cycles?','A visited set or another cycle-prevention rule',['A larger printf buffer','A static return type','A sorted vertex list only'],'Following a cycle without marking progress can revisit the same vertices forever.'],
['For n=5, why can the test n==0 fail to stop a step of n-2?','The sequence passes from 1 to -1 without reaching zero',['Odd numbers cannot be parameters','Subtraction is forbidden in recursion','The compiler rounds n'],'Use an appropriate domain and base case, such as n<=0, when that matches the problem.'],
['Why is returning &local from a recursive call unsafe after that call ends?','The automatic object has ended its lifetime',['Each return copies the pointed-to object','Recursion automatically uses static locals','Addresses are valid until main ends'],'The pointer does not keep the local object alive.'],
['Why can a very deep tail-recursive function exhaust resources in C?','Tail-call elimination is not required',['Tail recursion is a syntax error','Each call must allocate a file','Tail calls cannot take parameters'],'Use an iterative version when bounded stack usage is required.'],
['Why does this recursive count always return zero?','The caller discards the child result and returns zero',['n is unsigned','The base case is missing','There are two syntax errors'],'Return 1+count(n-1) for a count of positive steps.','int count(int n) { if(n==0)return 0; count(n-1); return 0; }'],
['What should a mutually recursive pair have before either function is called?','Compatible declarations visible at each call',['The same name','Only global parameters','Identical bodies'],'Provide prototypes so calls are checked even when definitions come later.']
];bugs.forEach(x=>mc(15,3,'Bug Hunt',x[0],x[1],x[2],x[3],x[4]));
var advanced=[
['What is the time complexity of naive fib(n)=fib(n-1)+fib(n-2)?','Exponential in n',['Constant','Logarithmic','Always linear'],'It repeatedly recomputes overlapping subproblems.'],
['What does memoization change in naive Fibonacci?','It stores solved inputs so they need not be recomputed',['It removes every base case','It changes the Fibonacci sequence','It guarantees no memory use'],'With cached results, each of the n+1 argument values needs to be solved only once.'],
['If one call does constant work and recurses on n-1, what is its time complexity?','O(n)',['O(1)','O(log n)','O(n squared)'],'There are linearly many calls before reaching the base case.'],
['If one call does constant work and recurses only on n/2, what is its time complexity?','O(log n)',['O(n)','O(n squared)','O(2 to the n)'],'Repeated halving reaches a constant-size input after logarithmically many steps.'],
['A merge sort makes two half-size calls and merges in linear time. Its time complexity is:','O(n log n)',['O(log n)','O(1)','O(n cubed)'],'There are logarithmically many levels, each with total linear merging work.'],
['Which factorial form is tail recursive?','return f(n-1, acc*n);',['return n*f(n-1);','return f(n-1)+1;','return 1+f(n-1)+f(n-2);'],'The tail form has no multiplication or addition left after the child returns.'],
['What can replace recursion when implementing a tree traversal iteratively?','An explicit stack holding pending nodes',['A single printf call','An uninitialized pointer','A file extension'],'The explicit data structure records work that recursive calls otherwise remember.'],
['Why is postorder useful when freeing every node of a tree?','Children are processed before their parent is freed',['The root must be freed first','It sorts all keys automatically','It avoids NULL checks'],'After freeing a parent, its child pointers can no longer safely be read from that object.'],
['What is the time complexity of visiting every node once in a tree with n nodes?','O(n)',['O(log n) for every tree','O(1)','O(n factorial)'],'Each node contributes a bounded amount of work. Tree shape affects call depth.'],
['What is the worst-case recursion depth for an unbalanced BST with n nodes?','O(n)',['Always O(log n)','O(1)','O(n squared)'],'A one-sided chain has one active call per level.'],
['How many moves solve the standard three-peg Tower of Hanoi problem with 4 disks?','15',['4','8','16'],'M(0)=0 and M(n)=2*M(n-1)+1, so M(4)=15.'],
['Which proof strategy fits a recursive algorithm?','Prove base cases, then show smaller correct results imply the larger result',['Test one input only','Assume every recursive call terminates','Count source lines'],'The inductive argument needs a decreasing measure as well as correctness of each step.'],
['What is the key distinction between backtracking and merely descending recursively?','Backtracking restores a prior choice before trying an alternative',['Backtracking never returns','Backtracking requires global variables','Backtracking removes base cases'],'Undoing temporary state lets sibling branches start from the right state.'],
['For a palindrome check on inclusive indices, which recursive step is appropriate after equal endpoints?','Check lo+1 through hi-1',['Check the unchanged range','Always check lo-1 through hi+1','Return false for every two-character range'],'The range shrinks toward the base case lo>=hi.'],
['For recursive Euclid on nonnegative a and positive b, what establishes progress?','a%b is less than b',['The first argument always increases','Both arguments are constant','The number of digits doubles'],'The second argument decreases until it becomes zero.'],
['Why is a single successful trace not enough to establish correctness?','Other inputs can miss the base case or violate assumptions',['Recursion is always random','A trace proves all input sizes','Only iterative programs need tests'],'Include empty, boundary, invalid-domain, and normal cases, and reason about termination.']
];advanced.forEach(x=>mc(15,4,'Concept',x[0],x[1],x[2],x[3]));

// Four focused additions per existing chapter, including appendix material.
var extra={
1:[['Which is a useful boundary test for accepting ages 0 through 120?','Test -1, 0, 120 and 121',['Test 50 four times','Test only letters','Skip zero'],'Tests just inside and outside the limits expose boundary mistakes.'],['What does an algorithm need before implementation?','Clear inputs, outputs, and finite steps',['A specific editor','A global variable','A Pokémon type'],'A design can be checked before choosing C syntax.'],['What distinguishes a logic error from a syntax error?','The program can compile yet compute the wrong result',['Logic errors always prevent compilation','Syntax errors only occur at runtime','There is no distinction'],'Passing compilation does not establish correctness.'],['How should a flowchart represent a yes/no decision?','A branch with labeled outcomes',['An unlabeled end node','Only a straight arrow','A variable declaration'],'Both outcomes need a defined next step.']],
2:[['Which format prints a size_t value in C99 and later?','%zu',['%d','%f','%p'],'sizeof produces size_t; the variadic format must match that type.'],['Which is guaranteed about sizeof(char)?','It is 1',['It is 8','It equals sizeof(int)','It is 0'],'A C byte is the size of char. CHAR_BIT gives its number of bits.'],['In C99 through C17, which header supplies bool, true and false macros?','<stdbool.h>',['<stdio.h>','<string.h>','<math.h>'],'The underlying language type is _Bool; the header provides convenient names.'],['Which identifier is reserved for the implementation everywhere?','__buffer',['buffer2','my_buffer','Buffer'],'Names beginning with two underscores are reserved; do not use them for application identifiers.']],
3:[['What does 1u << 3 evaluate to?','8',['3','6','9'],'The unsigned value 1 is shifted left three positions.'],['Which expression avoids truncation when averaging two int values a and b?','((double)a + b) / 2.0',['(a+b)/2','(double)((a+b)/2)','a/2+b/2'],'Conversion before addition and division avoids integer truncation and integer addition overflow.'],['Does C specify which argument is evaluated first in f(g(),h())?','No',['g always runs first','h always runs first','Both run in parallel'],'Do not make correctness depend on the evaluation order of function arguments.'],['Why parenthesize parameters and the whole expression in a function-like macro?','To reduce precedence surprises after expansion',['To allocate memory','To make it a real function','To avoid every side-effect problem'],'Parentheses help precedence, but repeated evaluation remains a separate macro hazard.']],
4:[['What does argc count in a hosted command-line main?','The number of argument strings supplied',['The number of characters in argv','Always exactly two','Only numeric arguments'],'argv[argc] is a null pointer; argc can be zero, so check before using argv[0].'],['What does int (*fn)(int) declare?','A pointer to a function taking int and returning int',['A function returning int pointer','An array of functions','An int pointer pointer'],'Parentheses bind the pointer declarator before the function parameter list.'],['Can changing a pointer parameter itself update the caller\'s pointer variable?','No, the parameter is a copy',['Yes, all pointers are references','Only when it is NULL','Only for char pointers'],'Pass a pointer to that pointer if the caller\'s pointer must change.'],['In C99 through C17, what does int f(void) specify?','No parameters',['Unspecified parameters','One void object','Any number of arguments'],'An explicit void parameter list gives a no-argument prototype.']],
5:[['What is the value of !!-9?','1',['-9','0','9'],'Logical negation produces 0 or 1; applying it twice normalizes truth.'],['Which makes checking p safe before evaluating *p?','p != NULL && *p == 7',['*p == 7 && p != NULL','p != NULL & *p == 7','*p == 7 || p == NULL'],'Short-circuit && skips the right operand when p is NULL.'],['What does default in a switch handle?','Values matching no case label',['Only zero','Only negative values','Every case before break'],'The default label is optional and is selected when no case matches.'],['Which data need explicit consideration when designing numeric branches?','Boundary values and invalid inputs',['Only positive odd inputs','Only the middle of the range','Only random values'],'Boundary tests help verify that adjacent branches meet without gaps or overlap.']],
6:[['What happens if the condition is omitted from for(;;)?','It is treated as true',['The loop never runs','It is a syntax error','It runs exactly once'],'The loop can still terminate through break, return, or another exit.'],['What is an invariant useful for?','Stating what remains true at a chosen point on every iteration',['Guaranteeing constant execution time','Replacing all tests','Making variables immutable'],'An invariant connects initialization, each loop step, and the final result.'],['Where does continue transfer control in a for loop?','To the iteration expression, then the condition',['Directly outside the loop','Back to initialization','Always to main'],'The update expression still executes, unlike a body-local update skipped in a while loop.'],['When calculating an average, why test the count before dividing?','The input may contain zero values to average',['Division cannot use variables','The count must be negative','Averages always require recursion'],'An empty data set needs an explicit policy instead of division by zero.']],
7:[['What does getchar return at end-of-file?','EOF as an int',['A char guaranteed to be -1','A NUL byte','Always zero'],'Store the result in int so EOF can be distinguished from a returned unsigned-char value.'],['Why should isalpha receive EOF or a value converted to unsigned char?','Other negative arguments cause undefined behavior',['It accepts only uppercase ASCII','It always changes its argument','It requires a pointer'],'For a possibly signed char c, use isalpha((unsigned char)c).'],['What should follow a failed scanf numeric conversion in a retry loop?','Handle or discard the offending input before retrying',['Retry unchanged forever','Use an uninitialized result','Close stdout'],'A matching failure can leave the same unread character in the stream.'],['What does fgets return when it fails to read any characters at EOF?','NULL',['An empty string always','EOF cast to char','The length -1'],'Check the return value before treating the buffer as a newly read line.']],
8:[['For int a[2][3], what is the type after a converts to a pointer?','Pointer to an array of 3 int',['int **','Pointer to an array of 2 int','int'],'Its elements are rows, each an array of three int.'],['Which search requires an ordered array for its usual correctness argument?','Binary search',['Linear search','Counting all elements','Finding the maximum by scanning'],'The ordering tells the algorithm which half can be discarded.'],['What does a partially initialized int a[4]={7} contain?','7, 0, 0, 0',['7 and three indeterminate values','Four 7s','It is invalid C'],'Unspecified elements in an aggregate initializer are zero-initialized.'],['How many comparisons does selection sort use on n distinct elements in its usual form?','n*(n-1)/2',['n','log2(n)','n factorial'],'Successive scans compare n-1, n-2, down to 1 remaining candidates.']],
9:[['What must be checked before malloc(n * sizeof *p)?','That multiplication fits size_t and allocation succeeds',['Only that n is odd','Only that p is global','That sizeof *p equals 4'],'Overflow can request too small a block. A NULL result must also be handled.'],['What does free(NULL) do?','Nothing',['Always crashes','Frees every allocation','Returns a new pointer'],'The C library explicitly permits a null pointer argument to free.'],['For positive newSize, what does realloc failure leave intact?','The original allocated object',['No object','Only its first byte','Only the pointer variable name'],'Keep the original pointer until a non-NULL replacement is obtained.'],['Can a pointer one past an array be dereferenced?','No',['Yes, it refers to a spare element','Yes, if the array is static','Only when the index is printed'],'It can be formed and used as an endpoint, but it does not point to an element.']],
10:[['What must fit when appending src with strcat?','Both strings plus one terminating NUL in the destination',['Only strlen(src)','Only the first character','Two pointer values'],'Check existing length, source length, and destination capacity before writing.'],['Can strlen determine the capacity of a char buffer?','No',['Yes, it includes spare bytes','Only for local arrays','Only after strcat'],'strlen counts characters before the first NUL, not allocated storage.'],['Which action is invalid for char *p="cat"?','Writing p[0]=\'b\'',['Reading p[0]','Calling strlen(p)','Assigning p to another string literal'],'Modifying a string literal is undefined behavior. Use a writable array for editing.'],['What does strcmp returning a negative value mean?','The first string compares before the second',['Exactly one character differs','The first string is always shorter','A read failed'],'The magnitude need not be -1; test the sign, zero, or positivity.']],
11:[['What does struct assignment copy?','Member values, including pointer values',['Every pointed-to allocation recursively','Only the first member','Nothing without memcpy'],'Copying a pointer does not duplicate the object it points to.'],['Can sizeof(struct S) exceed the sum of its member sizes?','Yes, because of padding',['No','Only if it has functions','Only for unions'],'Alignment requirements can introduce internal and trailing padding.'],['What does typedef create?','An alias for a type',['A new variable','A macro that always allocates','An automatic conversion function'],'It gives a type another name without creating an object.'],['Why keep an enum tag beside a union in a variant record?','To track which member the program intends to use',['To prevent any padding','To make all members active at once','To force one-byte storage'],'The program must maintain the tag and union member consistently.']],
12:[['What does a short fread item count require you to check?','EOF or a read error',['Only the filename length','Whether every byte equals zero','Only the file extension'],'Use feof and ferror after the read to distinguish the cause.'],['Is ftell on a text stream necessarily a byte count?','No, it can be a position token usable with fseek',['Yes, on every C implementation','It always counts lines','It returns size_t'],'Text-stream positions are not portable file-size measurements.'],['When can raw struct bytes be a portable interchange format?','Only with additional representation guarantees or explicit serialization',['Always if both files end in .bin','Whenever fwrite succeeds','Whenever the member names match'],'Padding, sizes, byte order, and representation can differ between implementations.'],['After output on an update stream, what is needed before input?','A flush or file-positioning operation',['Nothing ever','Only another printf','A new pointer name'],'C specifies sequencing rules when switching between reading and writing an update stream.']],
13:[['Which shift count is invalid for a promoted left operand with width W?','A count greater than or equal to W',['Zero','One when W is greater than one','W-1'],'Negative counts and counts at least the operand width cause undefined behavior.'],['What does x & ~mask do for unsigned operands?','Clear the selected bits',['Toggle all selected bits','Set every selected bit','Rotate the value'],'The complement mask has zeros in the positions to clear.'],['Are uint32_t and the other exact-width typedefs required on every implementation?','No, they exist only when a suitable exact-width type exists',['Yes, every machine must have them','Only uint32_t is mandatory','They always name unsigned long'],'Use the facilities of stdint.h with awareness of optional exact-width types.'],['Why can unsigned char operands still surprise you in bit expressions?','Integer promotions can convert them to int',['They never promote','They are always 32 bits','They cannot be shifted'],'Reason about the promoted operand type, not just the declared type.']],
14:[['What must removing the head of a linked list update?','The caller\'s head pointer',['Only a local copy of head','Every unrelated allocation','The compiler symbol table'],'Return the new head or accept a pointer to the caller\'s head.'],['Which order safely frees a linked list?','Save next, free current, advance to saved next',['Free current, then read current->next','Free head twice','Advance without saving any nodes'],'Reading a freed node to find its successor is a use-after-free.'],['What is an abstract data type primarily specified by?','Its operations and observable behavior',['One fixed memory layout','Its variable names','The number of source files'],'A stack can have multiple representations while preserving its push/pop contract.'],['Which traversal naturally uses a queue?','Breadth-first traversal',['Recursive postorder only','Inorder only','A single-node free'],'A queue visits earlier-discovered levels before later ones.']]
};Object.keys(extra).forEach(ch=>extra[ch].forEach((x,i)=>mc(Number(ch),i+1,'Concept',x[0],x[1],x[2],x[3])));
})();
