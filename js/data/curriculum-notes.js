/* Accuracy corrections and concise study notes. Original exercise wording,
   aligned to the fourth-edition contents; not a transcription of the textbook. */
(function(){
window.CURRICULUM_CORRECTIONS=[];
var by={};Object.values(QBANK).flat().forEach(q=>by[q.id]=q);
function fix(id,fields){if(!by[id])throw Error('Unknown question '+id);Object.assign(by[id],fields);CURRICULUM_CORRECTIONS.push(id);}
fix('c1-05',{why:'Preprocessing handles directives, conditional inclusion and macro expansion on preprocessing tokens before translation continues.'});
fix('c1-55',{why:'Macro replacement happens before type checking. The expanded C expression is still checked by the compiler; a macro itself is not a typed variable.'});
fix('c2-08',{q:'An automatic local int declared without an initializer has:',c:['A guaranteed zero value','A guaranteed NULL value','An indeterminate value that must not be read before initialization','A mandatory compile-time error'],why:'Do not treat an uninitialized int as a predictable collection of old bytes. Reading it can cause undefined behavior.'});
fix('c2-25',{why:'A const object has a declared type and cannot be modified through that name. Macro replacement creates tokens whose resulting expressions are then type-checked.'});
fix('c2-27',{c:['char a[4]="cat"; and char b[3]={\'c\',\'a\',\'t\'};','char a[4]="cat"; and char b[4];','char a=\'c\'; and char b[]="c";','int a[3]; and int b[4];']});
fix('c2-38',{q:'In the ordinary hosted C programs used in this course, which function is the entry point?',why:'Hosted C defines program startup through main. Freestanding systems, such as some embedded environments, can use an implementation-defined entry point.'});
fix('c2-50',{code:'printf("%zu", sizeof("abc"));'});
fix('c4-23',{why:'C passes the pointer arguments by value. Writing through those pointers stores results into the caller\'s objects.'});
fix('c4-51',{q:'Complete the phrase: C passes every argument, including a pointer argument, by ___.',a:['value','by value','pass by value'],why:'A function receives a copy of each argument value. A copied pointer can still provide access to a caller-owned object.'});
fix('c5-22',{c:['if (1 <= x <= 10)','if (x >= 1 && x <= 10)','if (x >= 1 || x <= 10)','if (1 < x < 10)'],why:'Use two comparisons joined by &&. In 1<=x<=10, the first comparison produces 0 or 1, which is always <=10.'});
fix('c6-03',{q:'When designing a typical counter-controlled loop, which three components should you plan?',why:'Plan the initial state, continuation test and progress step. C also permits loops with omitted clauses or other exit mechanisms.'});
fix('c6-20',{why:'At i=2, continue skips the body-local increment, so i stays 2. A for loop runs its iteration expression on continue, but still needs a correct progress rule.'});
fix('c7-15',{q:'Assuming str is a writable char array, which listed call reads a bounded line, including spaces?',why:'fgets receives the array capacity and the stream. Check its return value and handle a line longer than the buffer; gets is not available in C11 and later.'});
fix('c7-43',{q:'Assume fopen succeeds. Which explicit cleanup should follow this output before returning from the function?',why:'Check fopen first, then check output and fclose results. fclose flushes buffered output and releases the stream; normal program termination also closes streams, but abnormal termination may lose output.'});
fix('c7-48',{q:'Assuming fgets succeeds, which character sequence is stored after the user types hi and presses Enter?',c:["'h', 'i', '\\0'","'h', 'i', '\\n' only","'h', 'i', '\\n', '\\0'","Only '\\0'"],a:2,why:'The newline fits and is stored, followed by a NUL terminator. That is four stored characters.'});
fix('c8-10',{why:'An array parameter is adjusted to a pointer parameter. That pointer is passed by value and refers to the original array elements.'});
fix('c8-11',{why:'Selection sort selects the smallest remaining value for each position. An implementation can skip exchanging an element already in the correct position.'});
fix('c8-32',{why:'The declared array keeps its size. A resizable allocation uses malloc and realloc, covered in Chapter 9.'});
fix('c9-23',{q:'Why does a pointer\'s referenced type matter?',why:'It determines the type used for dereferencing and the element size used for pointer arithmetic. C does not require all pointer types to have the same size or double to occupy eight bytes.'});
fix('c9-49',{q:'Why is it unsafe to read an object through an arbitrary cast pointer of an unrelated type?',c:['Every pointer cast is forbidden','Alignment and effective-type rules may be violated','Every cast produces NULL','Every cast copies the object'],a:1,why:'A cast does not remove alignment or effective-type restrictions. Character types have special permission to inspect object representations.'});
fix('c10-19',{q:'On success, what does realloc(p, newSize) do when newSize is positive?',why:'It returns the resulting allocation and preserves the first min(oldSize,newSize) bytes. Added bytes are uninitialized. On failure, it returns NULL and leaves the old object allocated.'});
fix('c10-35',{q:'Which call requests space for 20 doubles and sets the allocated bytes to all-bits-zero?',why:'calloc clears the bytes, but all-bits-zero is not guaranteed to be floating-point 0.0 or a null pointer representation.'});
fix('c10-44',{q:'For n>0 and a valid allocated pointer p, which pattern handles realloc failure without losing p?',why:'Keep the old pointer until a positive-size reallocation succeeds. A zero-size request has special rules and is excluded here.'});
fix('c13-04',{q:'For a binary stream supporting positioning, what does a successful ftell return?',why:'For a binary stream, ftell gives the file-position indicator measured in characters from the start. It can fail with -1L. On a text stream it need not be a byte count; do not assume SEEK_END is universally supported.'});
fix('c13-05',{why:'Text mode can translate characters such as line endings; binary mode preserves the stream representation. Raw numeric sizes vary, and a binary format can be either explicitly serialized or implementation-dependent.'});
fix('c13-12',{why:'Raw object representations depend on the implementation. A defined binary serialization format can be portable; text formats also need agreed encoding and syntax.'});
fix('c13-45',{q:'On a seekable binary stream, 250 complete 40-byte records are read successfully from position 0. What does a successful ftell then return?',why:'The reads advance the binary file position by 250*40=10000 characters. Text-stream positions do not have this general byte-offset interpretation.'});
fix('c13-46',{q:'Which C function returns a long file-position value, or -1L on failure?',why:'ftell reports a position. Binary positions count characters; text positions should be treated as tokens for a later fseek with SEEK_SET.'});
fix('c13-63',{q:'Assume a seekable binary file consists of exactly 40 records of 25 bytes and supports SEEK_END without trailing padding. What does a successful seek to its end followed by ftell return?',why:'Under the stated file assumptions, 40*25=1000 bytes. This is not a portable file-size recipe for every possible C stream.'});
fix('c14-21',{why:'Unsigned types avoid signed-value shift pitfalls, but the shift count must still be nonnegative and less than the width of the promoted left operand.'});
fix('c14-35',{why:'Prefer unsigned integer operands for bit manipulation. Check integer promotions and shift-count bounds; unsigned shifts are not valid for every count.'});
fix('c14-52',{q:'In C99 through C17, why is shifting a negative signed int left not a safe substitute for multiplication?',c:['Shifts always multiply safely','Left-shifting a negative signed value is undefined behavior','Shifts convert it to double','Shifts always saturate'],a:1,why:'For example, -1*2 is representable, but -1<<1 has undefined behavior. Unsigned operations still require a valid shift count.'});

var notes={
1:['Separate hardware, system software and application software. Trace input through computation, storage and output.','Turn a problem statement into explicit inputs, outputs and constraints before writing C. Use pseudocode or a flowchart to inspect each branch and loop.','Distinguish preprocessing, compilation, linking and loading. A successful build does not prove that the algorithm is correct.','Use requirements, design, implementation, testing and maintenance as an iterative development process.','Distinguish required diagnostics for syntax/constraint violations from runtime failures and incorrect results. Not every runtime defect is detected.','Test empty, boundary, invalid and ordinary inputs. State the expected result before running each test.','Appendix practice: convert small values among binary, decimal and hexadecimal; trace labeled flowchart branches.'],
2:['Identifiers are case-sensitive. Avoid keywords and reserved names: double-underscore names and underscore-plus-uppercase names are reserved everywhere; other leading underscores have additional file-scope restrictions.','Initialize automatic objects before reading them. Uninitialized automatic int values are indeterminate, and using them can have undefined behavior. Static-storage objects receive zero initialization.','sizeof(char) is 1 C byte; CHAR_BIT tells you how many bits that byte has. Integer and pointer sizes are implementation-dependent, not always four or eight bytes.','Use matching formats: %d for int, %u for unsigned int, %zu for size_t, and %p with an object pointer converted to void *. For scanf, %f expects float * and %lf expects double *.','C99 through C17 provide _Bool and the <stdbool.h> bool/true/false macros. This course uses C99/C11/C17 unless an exercise says otherwise.','A const object has a type and cannot be assigned through a const-qualified lvalue. A macro expands tokens; the resulting C expression is still type-checked.','Hosted programs commonly use int main(void) or int main(int argc, char *argv[]). Freestanding startup rules can differ.'],
3:['Precedence groups operators; it does not impose a general evaluation order on operands. Use separate statements when sequencing matters.','Integer division truncates toward zero. Convert before division when a fractional result is required.','Post-increment yields the previous value; pre-increment yields the updated value. Unsequenced conflicting accesses, such as i++ + i++, have undefined behavior.','Unsigned arithmetic wraps modulo one more than the largest representable value. Signed overflow is undefined behavior.','Use parentheses to document grouping, but do not expect them to sequence side effects. Function argument evaluation order is not generally specified.','Appendix practice: parenthesize macro parameters and the replacement expression. Avoid side effects in arguments to a macro that uses its parameter more than once.'],
4:['Declare functions with compatible prototypes before calling them. In C99 through C17, f(void) specifies no arguments while an empty f() declaration does not give a parameter-type list.','C passes every argument by value. Passing a pointer value allows access to a caller-owned object, but changing the local pointer does not change the caller\'s pointer.','Use output-pointer parameters or return a structure for multiple results. Define preconditions such as non-NULL output pointers and valid numeric ranges.','Automatic locals belong to an invocation. Static locals persist across calls. Scope, storage duration and linkage answer different questions.','Appendix practice: argc counts argument strings and argv[argc] is NULL. Check argc before using optional arguments.','Appendix practice: int (*fn)(int) is a function pointer; int *fn(int) is a function returning an int pointer. Read declarations from the identifier outward.'],
5:['Zero is false and any nonzero scalar value is true in a condition. Relational and logical operators yield int 0 or 1.','Use && and || for short-circuit logic. Bitwise & and | evaluate both operands and are not substitutes when the right side may be unsafe.','Use x>=low && x<=high for a range. Chained comparisons are parsed as separate C operators and do not express a mathematical interval.','An else binds to the nearest unmatched if. Braces make the intended grouping explicit.','switch uses an integer controlling expression. Cases need unique integer constant expressions; execution falls through unless control leaves the switch.','Floating-point comparisons depend on the problem. When values come from approximate calculations, use an appropriate absolute/relative tolerance; exact comparison can still be appropriate for exactly represented sentinels.'],
6:['Identify initial state, a continuation rule, and a progress measure. A counter loop is one pattern, not a restriction on all loops.','while and for test before entering the body; do-while tests afterward. Distinguish an empty data set from a data set containing zero.','continue in a for loop runs its iteration expression before the next test. continue in a while loop can skip a body-local update.','break exits the nearest enclosing loop or switch. Use explicit control flow when leaving nested loops.','An invariant describes a fact preserved at a chosen point on each iteration. Combine it with a termination argument.','For(;;) is an intentional unbounded-loop form. Exit paths such as break or return may still terminate it. Avoid accidental stray semicolons.'],
7:['Check the result of each input operation before using its output. scanf returns the number of assignments, or EOF for input failure before a conversion.','Matching failure can leave unread input in the stream. A retry loop must consume or otherwise handle it to make progress.','Store getchar/fgetc results in int so that EOF can be distinguished from character values. Pass ctype functions EOF or a value representable as unsigned char.','fgets is bounded and retains a newline when one fits. Check for failure and decide how to handle a line longer than the buffer. gets was removed in C11.','Use a field width with scanf %s and reserve space for its NUL terminator. Use line input when spaces matter.','Check fopen, writes and fclose. Normal termination flushes streams, but explicit closing and error handling make ownership and failures clear.'],
8:['An n-element array has indices 0 through n-1. A declared array does not grow when an out-of-range index is used.','Unspecified elements in a partial aggregate initializer are initialized to zero. This differs from an uninitialized automatic array.','Most array expressions convert to pointers to their first elements; sizeof on an actual array still gives its full size. An array parameter is adjusted to a pointer.','A two-dimensional array is an array of rows, not int **. Preserve the row width when declaring a parameter that accesses those rows.','Trace sequential search, binary search, selection sort and insertion sort. State binary search\'s ordering precondition.','Practice empty and singleton ranges, first/last matches, missing values, duplicate values, and row/column bounds.'],
9:['Pointer type controls access and element-wise arithmetic. Do not assume all pointer representations or referenced type sizes are identical.','Dereference only a suitably aligned pointer to an object of an allowed type whose lifetime has not ended. NULL is a pointer value, not a usable object.','Pointer arithmetic is bounded by one array object, including its one-past endpoint. A one-past pointer must not be dereferenced.','Use a pointer-to-pointer or a returned pointer to replace the caller\'s pointer. Distinguish changing p from changing *p.','Before allocating n elements, check size multiplication for overflow and check allocation failure. sizeof *p follows the declared referenced type.','For positive-size realloc, keep the original pointer until success. On success, only the shared prefix is preserved; newly added bytes are indeterminate.','free(NULL) does nothing. Avoid double free and use-after-free; setting one pointer to NULL does not repair other aliases.','calloc sets bytes to zero, which is not a universal guarantee of floating zero or null pointer representations.','Appendix practice: void * supports conversions to and from object pointers; do not perform standard C arithmetic on void *. Function pointers have their own compatibility rules.'],
10:['A C string ends at its first NUL. Buffer capacity, stored byte count and strlen are different quantities.','A string literal must not be modified. char a[]="text" creates a writable array initialized from the literal.','Check destination capacity before copying or appending, including the final NUL. Source and destination overlap needs a function with appropriate overlap semantics.','strncpy does not guarantee termination when the source is too long. A length argument alone does not make every string operation safe.','strcmp promises negative, zero or positive, not specifically -1, 0 or 1. strlen requires an accessible terminating NUL.','Use bounded line input and checked numeric conversion; reject trailing junk and out-of-range values when the input contract requires it.'],
11:['An enum names integer constants. A typedef creates a type alias, not a variable.','Structures group members in declaration order, with possible padding between members and at the end. Do not assume sizeof equals the sum of member sizes.','Structure assignment copies member values. Pointer members still refer to the same objects unless you implement a deep copy.','A union shares storage among members. Use a tag to track the representation your program intends to read.','A self-referential structure can contain a pointer to its own type, but not a direct instance of itself as a member.','Use designated initializers to make member intent clear. Compare semantic fields instead of treating padding bytes as meaningful data.'],
12:['fread and fwrite report complete item counts. Check short results; after a read, distinguish EOF from ferror.','Binary mode avoids text translations, but writing raw objects is not automatically a portable file format. Define field sizes, encoding and byte order for interchange.','fseek reports success or failure. ftell returns a long position or -1L on failure; text-stream positions are not general byte counts.','Do not assume every stream supports seeking or SEEK_END. Compute record offsets only when the file layout, stream support and offset range are known.','Update streams have read/write transition rules. Output followed by input needs a flush or positioning operation; input followed by output generally needs positioning unless the read encountered EOF.','Never process a partially read record as complete. Write loops around successful transfers, not while(!feof(fp)).'],
13:['Use &, |, ^ and ~ for bit operations. Distinguish them from logical &&, || and !.','To set selected bits use x|mask; clear with x&~mask; toggle with x^mask; test with x&mask.','Check integer promotions. An unsigned char may be promoted to int before an operation.','A shift count must be nonnegative and less than the width of the promoted left operand. Unsigned type alone does not make every shift valid.','For the C99/C11/C17 course baseline, right-shifting a negative signed value is implementation-defined and left-shifting a negative signed value is undefined.','Exact-width types such as uint32_t are optional when no suitable type exists. Use <stdint.h>, <limits.h> and an explicit representation contract.'],
14:['Know the observable contracts of a list, stack and queue before selecting an array or linked representation.','Handle empty, singleton and multi-node lists. Inserting or deleting at the head may change the caller\'s head pointer.','Save the next pointer before freeing a node. Every allocated node needs an ownership rule and an eventual release path.','Stacks are LIFO; queues are FIFO. For a linked queue, keep head and tail consistent when the last element is removed.','A BST orders keys according to its comparison policy. Search time depends on height; a sorted insertion sequence can create a linear chain.','Distinguish preorder, inorder, postorder and breadth-first traversal. Graphs with cycles require a visited rule or equivalent safeguard.','Appendix practice: separate an abstract data type\'s operations from its representation and document how failure is reported.'],
15:['A recursive definition needs base cases and steps that move toward them for every permitted input. State the domain before tracing the calls.','Each recursive invocation has its own automatic locals. A static local is shared, so repeated top-level calls may affect one another.','Trace both descent and unwinding. Code before a child call runs on descent; code after it runs after the child returns.','Total invocation count and maximum active depth are different. Two sequential child calls create a branching call tree without executing in parallel.','Practice factorial, digit sums, Euclid\'s algorithm, string/array traversal, binary search, tree traversal and Tower of Hanoi. Check empty and boundary cases.','A tail-recursive call leaves no work in the caller after it returns. C does not guarantee tail-call elimination, so use iteration when bounded stack space matters.','For constant work per call, a single n-1 branch takes O(n) time; a single halving branch takes O(log n). Naive Fibonacci repeats subproblems and has exponential time.','Memoization stores solved subproblems. Backtracking explores choices and restores temporary state before exploring alternatives.','For recursive data structures, check NULL before dereferencing. To release a tree, free its children before freeing their parent.','Test termination, result range and storage use separately. A correct base case does not prevent signed overflow or excessive call depth.']
};CHAPTERS.forEach(c=>c.notes=notes[c.n]);
window.CURRICULUM_PRACTICE={
1:['Design a ticket-price calculator in pseudocode before writing C.','List valid inputs, reject negative ages, and draw each discount branch.','Check an age just below, on, and above every boundary. Keep test expectations separate from the code.'],
2:['Write a program that reads an item count and prints its size_t storage requirement.','Use initialized variables and matching input/output formats.','Reject negative or invalid counts. Use %zu for size_t and test count zero.'],
3:['Replace a square macro with a function and compare the behavior for an incrementing argument.','Use int square(int x) { return x*x; } for inputs whose result fits.','Do not execute an unsafe repeated-side-effect macro to guess its output. Explain why the function evaluates the argument once.'],
4:['Write a function that reports quotient and remainder through output pointers.','Specify nonzero divisor and representable quotient as preconditions, and store into *q and *r.','Test positive and negative dividends. Exclude INT_MIN/-1 when the result is not representable.'],
5:['Write a classifier for a value below, inside, or above an inclusive interval.','Use two explicit bounds; do not write chained comparisons.','Test low-1, low, a middle value, high, and high+1 where those arithmetic operations are representable.'],
6:['Read a sequence ending at a sentinel and compute its count and average.','Keep sum and count separate; the sentinel is not data.','Test sentinel first, one item, several items, and a failed numeric conversion. Do not divide by zero.'],
7:['Read a line containing spaces and report whether it fit into a fixed buffer.','Check fgets before examining the buffer, then handle a missing newline deliberately.','Test EOF before data, an empty line, a short line and a line longer than the buffer.'],
8:['Implement binary search on a sorted int array.','Choose and document inclusive or half-open bounds. Exclude the tested midpoint on each step.','Test empty, singleton, first/last match, missing key, and duplicates. State whether any matching index is acceptable.'],
9:['Implement a growable int buffer with a safe positive-size realloc step.','Check multiplication overflow, retain the original pointer on failure, and update capacity only on success.','Review failure paths without assuming allocation always succeeds. Release the final allocation exactly once.'],
10:['Write a checked string-append function taking destination capacity.','Check existing length and source length before copying the terminator. Define behavior on insufficient space.','Test empty strings, exact fit, one-byte-too-small capacity, and the overlap policy.'],
11:['Represent either a circle or rectangle using an enum tag and a union.','Keep the tag and stored member synchronized. Use a switch to compute the area.','Test each shape and an invalid tag. Explain why raw struct bytes are not a portable file format.'],
12:['Write and read records using a documented file representation.','Check every open, transfer and close. Distinguish incomplete input from a complete record.','Test empty and truncated files. If using raw objects locally, state the same-implementation assumption.'],
13:['Implement helpers to set, clear and test a bit in unsigned int.','Validate the bit index against the promoted operand width before constructing 1u<<index.','Test bit zero, the largest valid index, and an invalid index. Do not execute an out-of-range shift.'],
14:['Implement push, pop and destruction for a linked stack.','Define empty-stack behavior, allocate before linking, and save next before freeing.','Test an empty stack, one item, multiple items in LIFO order, and destruction after partial popping.'],
15:['Write recursive and iterative sums of the first n array elements.','For recursion, use n==0 as the base case and combine a[n-1] with the smaller result. State a no-overflow precondition.','Test empty, singleton and several-element arrays. Draw the active call chain for n=3 and compare auxiliary storage.']
};
})();
